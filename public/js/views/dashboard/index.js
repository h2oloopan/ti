// Generated by CoffeeScript 1.6.3
(function() {
  define(['text!templates/dashboard/index.html', 'models/User', 'widgets/addGoal'], function(template, User, WidgetAddGoal) {
    var IndexView;
    return IndexView = Backbone.View.extend({
      el: $('#content'),
      events: {
        'click .btn-add-goal': 'addGoal'
      },
      widgets: {
        wAddGoal: null,
        wProfile: null
      },
      render: function() {
        this.model = new User();
        this.model.url = this.model.urlRoot + '/self';
        this.model.on('change', this.onModelChange, this);
        return this.model.fetch();
      },
      onModelChange: function() {
        return this.$el.html(_.template(template, this.model.toJSON()));
      },
      addGoal: function() {
        if (!this.widgets.wAddGoal) {
          this.widgets.wAddGoal = new WidgetAddGoal($('#widget_add_goal'));
        }
        return this.widgets.wAddGoal.render();
      }
    });
  });

}).call(this);
