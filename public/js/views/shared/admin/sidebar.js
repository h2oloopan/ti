// Generated by CoffeeScript 1.6.2
define(['text!templates/shared/admin/sidebar.html'], function(template) {
  var SidebarView;

  return SidebarView = Backbone.View.extend({
    el: $('#sidebar'),
    events: {
      'click a': 'update'
    },
    render: function() {
      var hash;

      this.$el.html(template);
      hash = window.location.hash;
      return this.$('a[href="' + hash + '"]').parent().addClass('active');
    },
    update: function(e) {
      var li;

      li = $(e.currentTarget).parent();
      return li.addClass('active').siblings().removeClass('active');
    }
  });
});
