// Generated by CoffeeScript 1.6.3
(function() {
  var Sequelize, config, mysql, options;

  Sequelize = require('sequelize');

  config = require('../config');

  options = {
    host: config.db.host,
    port: config.db.port,
    define: {
      underscored: true,
      freezeTableName: true,
      timestamps: false
    }
  };

  mysql = exports.mysql = new Sequelize(config.db.database, config.db.username, config.db.password, options);

}).call(this);
