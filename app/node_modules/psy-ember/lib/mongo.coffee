mongoose = require 'mongoose'

#create a schema object that mongoose can use
#here we use the mongoose schema as the standard schema
parse = (schema) ->
	return schema

exports.createModel = (name, schema) ->
	createdSchema = new mongoose.Schema parse(schema)
	model = mongoose.model name, createdSchema
	return model