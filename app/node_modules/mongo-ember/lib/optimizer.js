// Generated by CoffeeScript 1.7.1
var compile, exec, fs, minifier, optimizer, path, uglify, vm;

exec = require('child_process').exec;

path = require('path');

fs = require('fs');

vm = require('vm');

uglify = require('uglify-js');

minifier = require('html-minifier');

compile = function(folder, cb) {};

optimizer = module.exports = {
  optimize: function(options) {
    var compileHandlebars, excludePath, folder, minifyJS;
    compileHandlebars = options.compileHandlebars || false;
    minifyJS = options.minifyJS || false;
    folder = options.publicFolder || null;
    excludePath = options.excludePath || null;
    if (compileHandlebars) {
      return compile(folder, function(err) {
        if (err) {
          return console.log(err);
        } else {
          return console.log('Handlebars compiled...');
        }
      });
    }
  }
};
