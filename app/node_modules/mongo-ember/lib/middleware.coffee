

createJs = (schema) ->
	js = ''
	#parse schema and create corresponding js

#this middleware is used to route js apis
module.exports = 
	addModel: (model) ->
		console.log model
	func: (req, res, next) ->
		next()